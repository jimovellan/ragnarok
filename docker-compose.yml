services:
  postgres:
    image: pgvector/pgvector:pg16
    container_name: rag-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-Jim}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-EnLaHoraMasOscura@1980}
      POSTGRES_DB: ${POSTGRES_DB:-docs}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-Jim} -d ${POSTGRES_DB:-docs}"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  pgdata:
